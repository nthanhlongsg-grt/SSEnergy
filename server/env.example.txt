# Server — production example for https://baohanh.sgesolartech.vn/
PORT=3000
NODE_ENV=production

JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=7d

DATABASE_PATH=./database/SGE.db

# Frontend origin (comma-separated if multiple)
CORS_ORIGIN=https://baohanh.sgesolartech.vn

TZ=Asia/Ho_Chi_Minh

# Optional — local seed scripts only (never commit real values)
# DEV_SEED_PASSWORD=
# DEV_SEED_EMAIL=developer@local.dev
# DEMO_SEED_PASSWORD=
