cd ~
export PATH="$HOME/.nodenv/versions/18/bin:$PATH"

# Xóa thư mục cũ, clone mới
rm -rf ~/SSEnergy
git clone https://github.com/nthanhlongsg-grt/SSEnergy.git SSEnergy

# Build
cd ~/SSEnergy/server
npm install
npm run build

# Copy dist mới vào app đang chạy
mkdir -p ~/baohanh.ssenergy.vn/sse-api/dist
cp -r ~/SSEnergy/server/dist/* ~/baohanh.ssenergy.vn/sse-api/dist/

# Dọn dẹp
rm -rf ~/SSEnergy

# Restart Node
ps aux | grep node
# kill -9 <PID>   hoặc Restart trên Plesk

# Kiểm tra
curl -s http://127.0.0.1:3000/api/health
