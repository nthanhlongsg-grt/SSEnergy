cd ~
export PATH="$HOME/.nodenv/versions/18/bin:$PATH"

# Xóa thư mục cũ, clone mới
rm -rf ~/SSE
git clone https://github.com/nthanhlongsg-grt/SSE.git SSE

# Build
cd ~/SSE/server
npm install
npm run build

# Copy dist mới vào app đang chạy
cp -r ~/SSE/server/dist/* ~/baohanh.sgesolartech.vn/sge-api/dist/

# Dọn dẹp
rm -rf ~/SSE

kill process

ps aux | grep node

kill -9 3498183